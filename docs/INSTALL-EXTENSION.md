# Установка Chrome Extension — пошаговая инструкция

## Шаг 1 — Запусти backend

```bash
cd /Users/vova/Documents/GitHub/recruiter-ai-coach/backend
npm start
```

Должно появиться:
```
🚀 Recruiter AI Coach backend running on port 3000
[Telegram] Bot started
```

Оставь этот терминал открытым.

---

## Шаг 2 — Загрузи расширение в Chrome

1. Открой Chrome и перейди по адресу:
   ```
   chrome://extensions/
   ```

2. Включи **Developer mode** (переключатель в правом верхнем углу)

3. Нажми кнопку **"Load unpacked"** (слева сверху)

4. Выбери папку:
   ```
   /Users/vova/Documents/GitHub/recruiter-ai-coach/chrome-extension
   ```

5. Расширение появится в списке — **Recruiter AI Coach** 🎯

---

## Шаг 3 — Тест на Google Meet

1. Открой любой Google Meet звонок: https://meet.google.com

2. В правом верхнем углу страницы появится оверлей 🎯

3. Нажми **▶ Начать запись**

4. Разреши доступ к микрофону (браузер спросит один раз)

5. Говори — подсказки появятся прямо в оверлее и придут в Telegram

---

## Как выглядит в работе

```
┌─────────────────────────┐
│ 🎯 AI Coach        🔴  −│  ← красная точка = слушает
├─────────────────────────┤
│ ⏹ Остановить           │
│                         │
│ Транскрипция:           │
│ "работал с реактом два  │
│  года, знаю хорошо..."  │
│                         │
│ 💡 Подсказки:           │
│ ┌─────────────────────┐ │
│ │ ❓ Уточни — React   │ │
│ │ или Vue на последнем│ │
│ │ проекте?     14:32  │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

---

## Если что-то не работает

**Оверлей не появился на Meet:**
- Проверь что расширение включено в `chrome://extensions/`
- Обнови страницу Meet (F5)

**Нет подсказок после речи:**
- Убедись что backend запущен (`npm start` в папке `backend`)
- Проверь консоль браузера (F12 → Console) — там будут ошибки

**Микрофон не захватывается:**
- Chrome должен иметь доступ к микрофону: `chrome://settings/content/microphone`

**Telegram не получает подсказки:**
- Убедись что `TELEGRAM_CHAT_ID=1714048` в файле `.env`

---

## Обновление расширения

После изменений в коде:
1. Перейди в `chrome://extensions/`
2. Нажми 🔄 (кнопка обновления) на карточке расширения
3. Обнови страницу Meet

---

## Быстрый старт (копируй целиком)

```bash
# Терминал 1 — backend
cd /Users/vova/Documents/GitHub/recruiter-ai-coach/backend && npm start

# Терминал 2 — проверить что всё ок
curl http://localhost:3000/health
```

Потом открывай Meet и нажимай ▶ в оверлее.
